NOTES:

How to build:
    # build release
    make -j2 config=release -f Makefile
    # build debug (uses defaults)
    make

Makefile must be told where boost is.  Defaults set to:
    BOOST_INC = /usr/local/include/boost
    BOOST_LIB = /usr/local/lib

To run
    ./codility

